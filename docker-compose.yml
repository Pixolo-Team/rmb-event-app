services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: evento
    ports:
      - "5432:5432"
    volumes:
      - evento_postgres_data:/var/lib/postgresql/data

volumes:
  evento_postgres_data:
